{% extends 'base.html.twig' %}

{% block title %}Modifié {{ playlist.name }}{% endblock %}

{% block body %}
    <h1>Modifier {{ playlist.name }}</h1>

    {{ include('playlist/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_homepage') }}">Retour au site</a>

    {{ include('playlist/_delete_form.html.twig') }}
{% endblock %}
