{% extends 'base.html.twig' %}

{% block title %}Modifié {{ song.name }}{% endblock %}

{% block body %}
    <h1>Modifier {{ song.name }}</h1>

    {{ include('song/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_homepage') }}">Retour au site</a>

    {{ include('song/_delete_form.html.twig') }}
{% endblock %}
